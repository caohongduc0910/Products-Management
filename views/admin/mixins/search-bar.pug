mixin search-bar(keyword)
  form(id="forms-search")
    .input-group 
      input(type="text" 
      placeholder="🔍Nhập từ khóa..." 
      name="keyword" 
      class="form-control"
      value=keyword
      )
      .input-group-append
        button(class="btn btn-success" 
        type="submit"
        ) Tìm